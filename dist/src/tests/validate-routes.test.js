import test from "node:test";
import assert from "node:assert";
// describe("validate-routes", () => {
//   it("Valid User", () => {
//     assert.
//       });
// });
test("equals 2", () => {
    //   it.only;´
    assert.deepEqual({
        name: "carlota",
    }, {
        name: "carlota",
    });
});
